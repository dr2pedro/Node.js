version: '3.5'

volumes:
  mongo_data:
      name: mongo_data

networks: 
  databases:
  views:

services: 
  gateway:
    build:
      context: ./
      args:
      - src=gateway
    deploy:
      replicas: 1
    ports:
      - 2000:2000
    command: npm run dev
    networks: 
      - databases
      - views

  signin:
    build: 
      context: ./
      args:
      - src=signin
    ports:
      - 2221:2221
    command: npm run dev
    networks: 
      - databases
    deploy:
      replicas: 1

  signup:
    build: 
      context: ./
      args:
      - src=signup
    deploy:
      replicas: 1
    ports:
    - 2222:2222
    command: npm run dev
    networks: 
      - databases
    
      
  db:
    image: mongo
    deploy:
      replicas: 1
    container_name: db
    ports:
      - "27017"
    volumes:
      - mongo_data:/data/db
    networks: 
      - databases
    